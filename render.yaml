services:
  - type: web_service
    name: offline-arcade
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node server.cjs
    envVars:
      - key: NODE_ENV
        value: production
